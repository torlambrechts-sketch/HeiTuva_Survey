# HeiTuva — Decision Register (v0.2)

Status legend: **CONFIRMED** = decided by Tor · **DEFAULT** = Claude-recommended default, applied so the build can start; overrule with a one-line instruction and the plan/migrations adjust.

17 decisions. Q11, Q15 and Q17 are confirmed by Tor; Q11/Q15 shape verification scope and Q17 the security kernel. The rest are defaults shaping the build.

| # | Question | Decision | Rationale / consequence |
|---|----------|----------|--------------------------|
| Q1 | Tenancy | **DEFAULT: Multi-tenant SaaS from day one.** | Splash page has pricing; retrofitting tenancy is far costlier than building it in. `organizations` is the RLS root everywhere. |
| Q2 | Anonymity vs reminders | **DEFAULT: Invitation/answer decoupling.** | `survey_invitations.responded_at` records participation; `responses` for anonymous submissions carry no invitation id, no user id, no IP/UA, and hour-truncated timestamps. Reminders work; linkage is structurally impossible. Enforced by DB constraint + the single submission RPC. |
| Q3 | k=5 disableable? | **DEFAULT: Hard-on, non-disableable.** | The Personvern toggle renders as informational ("Alltid på"). A toggleable anonymity guarantee is not a guarantee. Applies to all aggregate reads via RPC — anonymous *and* named surveys' group breakdowns. |
| Q4 | Respondent identity | **DEFAULT: Tokenized email links only in v1.** | `survey_invitations.identity_provider` column (nullable) reserved for BankID/ID-porten later; duty **signing** records member + timestamp + content hash (audit-grade, not qualified signature) in v1. |
| Q5 | Auth | **AMENDED (Phase 6, Tor): self-serve signup IS in v1.** Supabase Auth with `signUp` + e-mail confirmation from the splash, then the existing `/kom-i-gang` org creation; e-mail invite and magic link stay for people joining an existing org; Entra ID SSO in Phase 6. | The original default read invitation-only, which would have made the splash's "Prøv gratis" panel a contact form. Tor chose the real flow. **Consequence, recorded rather than discovered later: `/` now has a public account-creation endpoint**, so rate limiting and Cloudflare Turnstile on it move from "Phase 6 hardening" to **launch blockers**, alongside the two controls already parked in Q14 (administrator MFA, leaked-password protection). All four belong to the same pre-launch gate. **Phase 6f status:** the app-side rate limiter (`lib/ratelimit.ts`) and the Turnstile seam (`lib/turnstile.ts`, widget on `/`) are built and inert until the two Turnstile keys are set on Vercel; Supabase Auth's own sign-up rate limit must be set on the project. The SSO toggle is now real (D82) and offers itself only once the Azure provider is configured in Auth. **Phase 7 (decision 2):** enforcement is org-wide but the organisation must always keep at least one active administrator who can sign in without SSO — `org_members.sso_exempt`, held by two triggers in migration 0029 and shown as "Kan logge inn uten Entra ID" on the Valg card. |
| Q6 | Email/SMS providers | **DEFAULT: Amazon SES `eu-north-1` (Stockholm) for email; SMS deferred to Phase 6 (LINK Mobility when enabled).** | Keeps the "EU/EØS only" promise honest. If you prefer Resend's DX and accept US processing for transactional mail, say so — it's a provider-adapter swap (`lib/mail/` interface). SMS channel UI ships behind a feature flag. |
| Q7 | AI features | **DEFAULT: v1 algorithmic.** | Free-text themes = keyword/stem grouping (as the prototype's `THEME_WORDS`); insights = rule-based (lowest questions, trend deltas, participation gaps). Anthropic-API theming/insights behind `feature_flags.ai_insights`, Phase 6, pending your data-processing posture on free text leaving Supabase. |
| Q8 | Benchmarks | **DEFAULT: Seeded static Norwegian reference values, panel footnoted with source.** | `benchmarks` table seeded; cross-tenant aggregation is a later product decision (needs DPA language). |
| Q9 | Import scope | **DEFAULT: CSV / Excel / paste real in v1; Entra ID, Google Workspace, HR-systems as stubs behind the same UI ("kommer").** | Same import UI as designed; sync sources gated by `feature_flags`. |
| Q10 | Exports & splash | **DEFAULT: PDF export in v1 (server-side, @react-pdf or Playwright-print); PPTX Phase 6. Splash built pixel-perfect in Phase 6 with pricing CTA = "Kontakt oss" (no Stripe in v1).** | "All parts of the design" honored; billing engine deferred until there's a paying pipeline. |
| Q11 | App languages | **CONFIRMED: Norwegian + English at launch.** Phase 6 addendum: the splash therefore offers `no` + `en` only and says "Norsk og engelsk", not the bundle's "Norsk, svensk, dansk og engelsk" — a public page must not claim a capability the product does not have. The splash prototype's `sv`/`da` copy is kept in the repo and activated with those languages (D74). | Both surfaces (admin app and respondent) ship no + en. sv/da strings from the design are seeded in `ui_messages` but the languages are inactive (`organizations.active_langs`) — enabling them later is a config change, not a build. |
| Q12 | Translation editing | **DEFAULT: Supabase Studio (direct `ui_messages` edits) initially; admin editor screen in Phase 6.** | Edits are cache-revalidated live either way. |
| Q13 | AI-assisted survey translation | **DEFAULT: Manual-only in v1.** | Statutory packs ship pre-translated as seed data (no/en). |
| Q14 | MFA for administrators | **DEFERRED (Tor): not enforced.** Do not build or enforce TOTP. Supabase Auth's MFA capability stays available; it is simply not required at login. **Re-enable trigger (unchanged): before the first real organisation is onboarded, or before prod holds any real respondent data — whichever comes first.** | Deferred on 2026-09-04 to cut development friction while there were no customers and no real data (D27). Briefly re-enabled in Phase 7 (7d) and reverted the same phase on Tor's instruction: the enforcement — `/sikkerhet`, the layout gate, the admin-action gates and the harness TOTP seeding — is removed again, back to deferred. Recorded as a deferral with a named trigger rather than a silent removal, because it is a security control and the register's point is that nothing lapses by default. Leaked-password protection is off for the same window; both belong to the same pre-launch gate. |
| Q15 | Viewport scope | **CONFIRMED: admin mobile support is in v1.** All surfaces responsive. Desktop (≥1280px) stays pixel-perfect to the design; tablet/mobile follow `docs/RESPONSIVE.md`, which is the specification for layouts the prototype does not contain. Respondent-facing surfaces stay mobile-first pixel-perfect. | The prototype is a fixed 1440px canvas with no mobile app layouts, so "pixel-perfect" cannot apply below 1280px — RESPONSIVE.md supplies decided patterns instead, and CLAUDE.md's do-not-invent rule is relaxed only to those patterns. Consequence: D12 (header overflow at 390px) is now a defect to fix, not an accepted deviation; verification captures app routes at mobile again, judged rule-based rather than against a reference image. |
| Q16 | Job queue | **DEFAULT: pgmq in Supabase.** The plan's earlier alternative (Inngest) is not being used. | Keeps the send pipeline inside the EU/EØS boundary and one operational surface. Revisit only if scheduling needs outgrow it. |
| Q17 | Display threshold | **CONFIRMED (Tor): k becomes a per-survey policy with a law-anchored floor.** `k=5` stops being a global constant and becomes a policy on each survey: default **5** unchanged (and 5 is what applies when nobody chooses), floor **3** for natural persons, **no threshold** for organisation respondents (attributed answers), and statutory packs carry their own **locked** policy the customer cannot override. The threshold and respondent type are free while the survey is a draft and lock at the first response (trigger, not screen); lowering below 5 states its consequence in the UI but takes no confirmation dialog. Changing the threshold is an administrator action, audited. `app.k_threshold()` becomes `app.k_for(p_survey)`; every aggregate path routes through it. Full model, migration sketch and the eight mandatory negative tests: **docs/Q17_terskel_forslag.md**; UI brief: **docs/Designbrief_terskel_Q17.md**. | This is the only change in the expansion work that touches the security kernel, so it gets its own decision, migration and negative tests before any code. Structural anonymity is untouched (no `invitation_id`/user/IP on anonymous rows, no client read on `responses`/`answers`); the threshold governs what is *shown*, not whether the linkage exists — it does not. **Scope of the first build: the policy model only.** The attributed-results view and the dashboard archetypes in the design brief are a later phase, once the model is proven. |

## Standing invariants (not decisions — never violated)
1. No client ever selects from `responses`/`answers`. Reads only via SECURITY DEFINER aggregate RPCs enforcing the survey's threshold per cell — `app.k_for` (default 5, floor 3 for natural persons, none for organisation respondents; see Q17), never a client-supplied value.
2. Anonymous responses can never reference an invitation, user, IP, or precise timestamp. DB CHECK constraint + RPC design.
3. All schema changes via migrations; RLS enabled on every table in the same migration that creates it.
4. Tokens stored hashed; compared constant-time; expiring.
5. EU/EØS residency: Supabase `eu-central-1`, EU-region providers only (see Q6), Sentry EU, no respondent free text in logs or error payloads.
6. Pixel-perfect to the design bundle tokens; deviations only where DECISIONS.md or the plan says so.
