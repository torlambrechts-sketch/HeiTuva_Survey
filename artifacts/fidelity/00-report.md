# Fidelity composites — 2026-09-14

Drawing: `artifacts/reference-v6` (35 screens). App: `artifacts/phase-*/*.desktop.png`.

28 composite(s) in `artifacts/fidelity/`. 7 screen(s) with no app capture:

- **live-revealed** — not in the capture manifest — the app reveals, no state declared
- **oppgaver-oppgaver** — not in the capture manifest — /oppgaver?type=oppgaver is built
- **oppgaver-tilbakemeldinger** — not in the capture manifest — /oppgaver?type=tilbakemeldinger is built
- **respondent-kommentar** — not in the capture manifest — the comment box is built
- **respondent-kommentar-lagret** — not in the capture manifest — the saved state is built
- **respondent-takk** — not in the capture manifest — the thanks screen is built
- **respondent-takk-sendt** — not in the capture manifest — the thanks screen is built

## What these can and cannot establish

They can show whether a screen looks like the drawing it is paired with. They cannot
say whether the drawing is what we decided to build — the decided-not-built list is a
decision and not drift — and they are **structurally blind to a surface the app never
built**, because a screen that does not exist produces no capture. Those appear above
as rows, which is the most this comparison can do about them.
