/**
 * Phase 0 placeholder. No UI ships until the invariant suite is green and
 * Phase 1 builds the real shell against /design-reference/. Do not style this
 * page — it is scaffolding, not a screen.
 */
export default function Page() {
  return (
    <main className="p-8">
      <h1 className="font-display text-2xl">HeiTuva</h1>
      <p className="text-mut">Fase 0 — fundament. Ingen grensesnitt ennå.</p>
    </main>
  )
}
