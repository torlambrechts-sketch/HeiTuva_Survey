# dependencies
node_modules/

# env / secrets
.env
.env.*
!.env.example

# next.js
.next/
out/

# vercel
.vercel

# supabase
supabase/.temp/
supabase/.branches/

# claude code local settings — records approved commands verbatim,
# which can include tokens. never commit.
.claude/settings.local.json

# misc
.DS_Store
*.log

# next.js build output
/.next/
next-env.d.ts
*.tsbuildinfo

# verification artifacts — captures are regenerated per run.
# Both reference sets ARE committed: they are the design baselines, one per
# handoff (DECISIONS Q18). artifacts/reference/ is the first bundle, the
# baseline Phases 1-7 were built against; artifacts/reference-v1/ is the
# second, the target for every screen a v1 phase touches.
artifacts/*
!artifacts/reference/
!artifacts/reference-v1/
artifacts/.cdn-cache/
test-results/
playwright-report/
.mfa-secrets.json
